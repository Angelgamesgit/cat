using UnityEngine;

public class CatAnimSound : MonoBehaviour
{
    [SerializeField]
    CatSystem parentSystem;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void CatAudio()
    {
        //parentSystem.CatFootStepCurrent();
    }
}
