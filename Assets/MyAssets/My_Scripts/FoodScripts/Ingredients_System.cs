using UnityEngine;

public class Ingredients_System : MonoBehaviour
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void OnTouch(Ingredients ingredients)
    {
        //このオブジェクトがタッチされた時の処理
        //猫のシステム側にスクリプタブルオブジェクトのデータを渡す処理をここに記述

    }
}
